# 已知缺口与延期项

以下内容当前明确未固化为实现事实，后续必须补充资料、需求和验证后再开发：

1. 具体选品条件、必填字段、默认值、排序、去重和业务阈值。
2. 选品结果的最终业务动作：仅生成候选、加入分销、提交发布，还是组合流程。
3. 慧策 HTTP API 的正式字段、分页、supplierShopId、商品/SKU 标识来源和响应样本。
4. 真实测试账号、授权测试资源和可回滚的隔离数据库/Profile。
5. 选品模块 SQLite schema、迁移策略、数据保留和清理边界。
6. BSClaw-Local 当前未提交工作区中服务 manifest/适配器的最终合法性与接入版本。
7. 自动执行没有业务确认后的风险策略、规则版本锁定和异常补偿方案。
8. 性能、限流、并发、超时、断网和外部任务长时间运行的基线。

在这些内容补齐前，本资料包只能支撑主干设计和契约拆解，不能宣称选品模块可交付。
