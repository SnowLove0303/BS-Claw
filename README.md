# BSClaw

BSClaw 是基于 Electron、TypeScript、React 和 Vite 的 Windows 桌面应用。

当前版本只包含最小可运行应用壳，不包含业务功能、后台服务、数据库、网络请求或占位入口。

## 开发

```powershell
npm install
npm run dev
```

## 验证与构建

```powershell
npm run typecheck
npm run build
npm run dist
```

所有运行数据、缓存和构建产物固定在 `F:\XIANGMU\BS Claw\System`。
