# 业务功能实际测试审计摘要

本次完成了独立副本中的自动化业务流程检查并记录证据。中文菜单、空库查询、参数校验和不存在资源拒绝路径可执行；但正常注册路径在未监听端口这一合法初始场景直接因 `.Count` 属性错误失败，导致核心业务链路被阻断。

本摘要仅作问题记录，不作打回或通过判定。后续应先针对 `BF-P0-001` 修复并复测，再继续真实慧策通端口、浏览器、页面、登录、超时、重启与删除保护闭环审计。

## 打回优化复验摘要

BF-P0-001、BF-P1-002 至 BF-P1-005、BF-P2-006 和 BF-P2-007 已完成代码修复与自动化复验。完整回归 15 项通过、0 项失败：

- 正常未监听端口可注册，并准确保存“不可连接/登录状态未知”。
- Edit/Delete 的成功和失败 stdout 均为单一 JSON，失败使用非零退出码。
- Check/Open 的最新失败状态可在后续 List/Detail 和状态文件中看到。
- 该历史轮启动的真实 Edge 在 Open 失败后无残留进程，租约为 0；当前轮已改用真实 Google Chrome，且禁止 Edge 回退。
- 公网和远程 DNS 主机被拒绝；私网资源只读检测可登记，Open 仅允许回环。
- Edit/Delete 在写锁内重新检查租约与监听。
- List/Detail 在租约期间显示真实租约占用，释放后恢复。

生产 `data\ports.json` 哈希未变化，`F:\XIANGMU\BS Claw\System` 保持原 HEAD 和干净工作树。

真实慧策页面成功打开、真实账号登录证据和 BS Claw 主系统加载仍未测试或未开发，不属于本次通过结论。

## 2026-07-28 首次操作路径优化摘要

BF-P0-016、BF-P1-017 至 BF-P1-024、BF-P2-025 已完成代码优化和开发自动化复验。最终完整回归 RG-001 至 RG-020 共 20 项通过、0 项失败：

- 菜单 1 已改为普通用户业务向导，资源名、端口、连接枚举、Chrome 路径、F 盘 profile 和匹配规则不再阻断首次注册。
- 程序优先发现真实慧策 Chrome；没有时自动检测 Google Chrome、分配真实空闲端口并创建 F 盘 profile，不回退 Edge。
- 注册后自动检查并提供立即打开、再次检查或返回菜单，不要求复制资源编号和命令。
- 默认运行数据固定在项目 `runtime`，源码 `data\ports.json` 哈希未改变，异常主文件可从备份恢复。
- 用户层保留真实分层证据，并只提供一项中文下一步动作。

本轮使用真实端口、真实监听进程和真实 Google Chrome 执行开发回归，但没有用户真实慧策页面与账号环境。因此当前结论是“优化实现及自动化回归完成，等待用户人工复测”，不是本次业务审计通过。

## 2026-07-28 登录状态模型与真实页面复验摘要

资源数据已升级为 schema v2，登录状态、登录证据、检测时间、Credential Ref 预留和会话策略与端口/Chrome/页面状态分离。当前适配器没有真实鉴权规则，故不会把端口、Chrome、页面标题或历史状态推断为“已登录”。

开发线程已用真实 Google Chrome 和独立 F 盘 profile 成功打开慧策登录页，得到“平台页面正确 / 未登录 / login-page-rule”；同时对用户已打开的真实慧策页面进行只读 CDP 判断，在没有鉴权证据时得到“登录状态未知”。测试后 Chrome 残留进程 0、租约 0，data/log 敏感信息扫描无发现。

真实账号鉴权证据、“登录已失效”、自动登录、Credential Ref 后端、SQL 落库和 BS Claw 主系统加载仍未开发或未验证。最终结论仍为“等待用户人工复测”，不判定业务审计通过。
