# BSClaw 端口管理第一阶段双重审计范围

审计对象：`F:\XIANGMU\BS Claw\PortManager-Phase1`。

审计维度：

1. 代码审计：入口参数、PowerShell 模块边界、持久化、状态模型、端口与 Chrome 生命周期、租约锁、错误处理、凭据安全、Chrome-only 约束和运行目录隔离。
2. 业务流程审计：注册、列表、详情、编辑、启停、删除、状态检测、全量检查、打开指定慧策通端口、失败反馈、并发占用、异常恢复和真实登录状态闭环。

本次不把静态回归或无账号环境结果推定为真实慧策业务通过。真实账号登录、已登录鉴权证据、登录失效、自动登录和会话复用仍必须标记为未验证。

本次审计必须按完整业务选项覆盖，而不是只复核已有自动化用例。覆盖范围至少包括：菜单 0–8 及非法输入；注册的已打开慧策 Chrome、自动启动 Chrome、高级设置、默认回车、无效输入、重复与并发竞态；列表和详情的空库、多资源、资源不存在；编辑的单字段、全字段、空变更和无效值；启用、停用及停用后打开；删除的无确认、错误确认、占用拒绝和成功回收；单项检查与全部检查的空库、多资源、部分失败；打开流程的已连接页面激活、无页面新建、Launch、ConnectOnly、远程拒绝、端口冲突、超时、浏览器退出和回收；登录页、已登录页、登录失效、未知和检测失败；租约过期、并发写入、异常退出、重启恢复、审计、敏感信息、JSON 契约、Text 交互和运行目录隔离。
