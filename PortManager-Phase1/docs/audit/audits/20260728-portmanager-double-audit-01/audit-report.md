# BSClaw 慧策通端口管理第一阶段双重审计报告

审计日期：2026-07-28

审计对象：`F:\XIANGMU\BS Claw\PortManager-Phase1`

审计结论：上轮报告覆盖范围不足，不能作为全流程审计结论。本轮已补充完整业务覆盖矩阵，并将所有菜单选项、正常路径、异常路径、边界路径、并发路径、恢复路径和真实登录路径区分为已验证、部分验证、未验证。当前仍不能判定为完整通过。

## 按优先级汇总

P1：

- CA-P1-001：注册端口冲突检查未在写锁内最终复核。
- CA-P1-002 / BF-P1-001：没有正向鉴权证据，已登录、登录失效、自动登录和会话复用未形成可验收闭环。

P2：

- CA-P2-003：全机监听端口探测与 TCP 回退日志噪声较大。
- CA-P2-004：历史审计资料仍有 Edge 表述，与当前 Chrome-only 规范不一致。
- BF-P2-002：文本失败反馈仍可能暴露底层技术语义，用户下一步理解成本偏高。
- BF-P2-003：本次重新执行回归脚本未生成完整汇总文件。
- BF-P1-004：活动 Chrome 占用时的错误下一步被误报为 Chrome 安装问题。
- BF-P1-005：错误确认删除未给出精确确认格式的下一步。

本轮已用真实 Chrome 和真实慧策页面执行注册、识别、Detail、List、Open 页面激活回查、关闭后 Check、Edit、Disable、停用后 Open 拒绝、Enable、错误确认删除、正确删除，并以两个真实隔离资源执行 CheckAll。未闭环的流程详见 [全业务流程覆盖矩阵](coverage-matrix.md)，剩余重点是正向已登录鉴权证据、登录失效、自动登录、已有多个慧策 Chrome 的选择、高级设置全字段、租约过期、应用重启恢复、无页面新建和注册并发竞态。

## 审计判定

代码审计：基础安全、F 盘运行目录、Chrome-only、凭据不落盘、租约锁、失败状态和进程回收方向通过；注册竞态与正向登录能力缺口未通过。

业务流程审计：注册、管理、检测、占用保护、失败回收和隔离运行路径具备回归证据；真实慧策登录、登录后回查和后续业务使用路径为“未验证”，不得标记通过。

## 给技术线程的处理顺序

1. 先修 CA-P1-001，确保注册最终判定在写锁内复核真实端口占用和 CDP 身份。
2. 明确正向鉴权证据来源并实现可复核的已登录、登录失效状态机，同时保持 Credential Ref 和敏感信息不落盘。
3. 修复回归脚本的异常捕获和最终汇总，任何中断都必须落盘 summary。
4. 降低端口扫描和 TCP 回退日志噪声，清理历史审计中的过时浏览器表述。
5. 完成真实 Chrome/慧策账号人工实机复验后，再进行下一轮双重审计。
