# 后续复测要求

1. 先修复并单独验证 BF-P2-014，确保自动回归始终生成最终汇总。
2. 优化 BF-P2-015 的端口拥有者检测日志分类和限频策略。
3. 在真实慧策通已登录的 CDP 端口上重新执行注册、Check、Open、页面回查和登录状态边界。
4. 记录真实账号、页面、权限和后续业务动作的完整人工复测路径；凭据只使用 Credential Ref，不写入审计文件。

## 浏览器执行策略更新

根据后续平台约束，当前及后续自动回归、热试车和默认浏览器发现仅允许使用 Google Chrome；Edge 不再作为默认候选或回退候选。历史审计中的 Edge 仅表示当时的事实证据，不作为 Chrome 验收证据。若现场未安装 Chrome，浏览器启动相关项必须标记为“未验证”，不得改用 Edge 代替。
